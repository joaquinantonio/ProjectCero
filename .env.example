# Rename to .env and fill in values
# Django / general
DJANGO_SECRET_KEY=replace-me-with-a-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Postgres DB (local dev)
DB_NAME=projectcero_dev
DB_USER=projectcero
DB_PASSWORD=changeme
DB_HOST=127.0.0.1
DB_PORT=5432

# Email (optional)
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=False

# Application emails
DEFAULT_FROM_EMAIL=CeroPJ <no-reply@example.com>
BOOKING_NOTIFICATION_EMAIL=
ORDER_NOTIFICATION_EMAIL=

